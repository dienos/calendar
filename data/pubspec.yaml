name: data
description: Data module for the calendar app.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain

  floor: ^1.5.0
  path_provider: ^2.1.2
  intl: ^0.20.2
  file_picker: ^8.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # For code generation within the data module
  build_runner: ^2.4.10
  floor_generator: ^1.5.0
